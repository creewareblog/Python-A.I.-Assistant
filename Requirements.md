To run assistant you have to install following modules from python terminal(pip):

1. Google Text To Speech (works online) 
2. Speech Recognition
3. Wikipedia API (optional)

Use below codes:

pip install gtts
pip install SpeechRecognition
pip install wikipedia
